{
  "private": true,
  "license": "MIT",
  "devDependencies": {
    "babel-eslint": "^8.0.1",
    "lerna": "^2.4.0",
    "standard": "^10.0.3"
  },
  "workspaces": [
    "packages/*"
  ],
  "standard": {
    "parser": "babel-eslint"
  },
  "scripts": {
    "build": "lerna run build --toposort --concurrency=1"
  }
}
