{
  "private": true,
  "license": "MIT",
  "devDependencies": {
    "babel-eslint": "^10.0.2",
    "eslint": "^6.0.1",
    "lerna": "^3.15.0",
    "standard": "^13.0.2"
  },
  "workspaces": [
    "prosemirror-*",
    "react-*"
  ],
  "standard": {
    "parser": "babel-eslint"
  },
  "scripts": {
    "build": "lerna run build --concurrency=1",
    "publish": "lerna publish"
  }
}
