{
  "private": true,
  "license": "MIT",
  "devDependencies": {
    "babel-eslint": "^8.2.1",
    "lerna": "^2.8.0",
    "standard": "^11.0.0"
  },
  "workspaces": [
    "prosemirror-*",
    "react-*"
  ],
  "standard": {
    "parser": "babel-eslint"
  },
  "scripts": {
    "build": "lerna run build --toposort --concurrency=1",
    "publish": "lerna publish"
  }
}
