// import { blockActive, markActive } from '@pompom/core'
// import { setBlockType, toggleMark } from 'prosemirror-commands'
//
// import { schema } from './schema'
//
// export const commands = {
//   toggleMark: Object.fromEntries(
//     Object.entries(schema.marks).map(([key, value]) => [key, toggleMark(value)])
//   ),
//   markActive: Object.fromEntries(
//     Object.entries(schema.marks).map(([key, value]) => [key, markActive(value)])
//   ),
//   setBlockType: Object.fromEntries(
//     Object.entries(schema.nodes).map(([key, value]) => [
//       key,
//       setBlockType(value),
//     ])
//   ),
//   blockActive: Object.fromEntries(
//     Object.entries(schema.nodes).map(([key, value]) => [
//       key,
//       blockActive(value),
//     ])
//   ),
// }
